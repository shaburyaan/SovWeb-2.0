Roboto/media pass completed.

- Swapped UI typography to `Roboto` with `Noto Sans Armenian` fallback.
- Replaced header wordmark with the Sovrano logo image and tightened cursor follow.
- Added manifest-backed `optimized-media` webp pipeline for homepage, partners, detail, and distribution media.
- Enlarged clipped media slots, switched wide surfaces to local `contain`, and revalidated `npm run build:pages`.
