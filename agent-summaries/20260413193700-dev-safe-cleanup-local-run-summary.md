Safe cleanup/local-run completed.

- Proved legacy parity: `129/129` migrated, zero unresolved gaps.
- Archived `modern-app`, `test-results`, `optimization-*`; deleted legacy route HTML.
- Added `runLocal`, `dev:sov`, parity/cleanup scripts, and rewired PowerShell `devSov`.
- Validated with `npm run lint`, `npm run build`, `runLocal`, and `devSov`.
