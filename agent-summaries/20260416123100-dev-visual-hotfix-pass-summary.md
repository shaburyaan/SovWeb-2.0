# Visual hotfix pass

- Rebuilt `root-experience` light stacking and darkened false-white CSS surfaces so homepage glow is readable and cards/media stay black.
- Routed homepage partners + shared `ParallaxMedia` through optimized-media, restored stable english brand routing via root `[slug]` plus explicit `hy/ru` locale pages, and hardened transition overlay to a single non-remounting layer.
- Extended media cleanup for `wp-content/uploads/2022/11` Russian Standard assets, regenerated targeted optimized files/mappings, and live-validated `localhost:3000`, `/our-partners`, `/garofalo`, `/virgilio`.
