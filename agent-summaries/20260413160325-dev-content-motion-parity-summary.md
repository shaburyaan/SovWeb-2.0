# Dev content motion parity

- Restored `Our Partners` from partial grid to full tab-driven dataset using raw blocks, raw images/links, and supplemental partner cards sourced from local/live parity gaps.
- Added controlled typing retriggers, scrub-capable scene hooks, locale-safe route normalization, `/en|hy|ru` detail routes, and generic fallback rendering for dynamic content pages.
- Rebuilt cleanly after clearing stale `.next`; validated `build`, core internal routes, partner routes, detail routes, and fresh screenshots on `http://localhost:3002`.
