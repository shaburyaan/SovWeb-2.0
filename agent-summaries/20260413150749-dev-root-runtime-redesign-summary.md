Root repo switched to Next.js 15 App Router runtime.
Added global cursor provider, Lenis scroll, page-transition shell, reveal typography primitives, homepage scenes, generic routed pages, and `public/` asset migration.
Validated with `npm run lint`, `npm run build`, local `localhost:3000` route checks for `/`, `/hy`, `/about-us`, `/distribution`.
