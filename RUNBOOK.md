# Runbook

2026-02-13 15:06 | Fix: restored partner logos on home pages (en/ru/hy) by removing `img[loading="lazy"] { opacity: 0; }` CSS.
2026-02-13 15:40 | Update: replaced Virgilio/Sterilgarda/Sacla texts (hy/en/ru), fixed hy partner-card mapping, removed Barilla mentions globally.
2026-02-13 15:56 | Fix: removed residual B&B partner card/logo from home carousel and partners pages (en/ru/hy).
2026-04-13 15:07 | Redesign: root runtime switched to Next 15 App Router; added global cursor/Lenis/transition shell, content adapters from `reports/*.json`, cinematic homepage and generic routed pages; validated with `npm run build` and local DevSov route checks.
2026-04-13 15:27 | Redesign: replaced generic internal pages with route-aware cinematic scenes for about/distribution/partners/vacancy/contact, moved clean dev runtime to `localhost:3001`, revalidated en/hy/ru routes and screenshots.
2026-04-13 16:03 | Parity: restored `Our Partners` full dataset with tab-driven motion, supplemental live/local partner cards, `/en|hy|ru` detail routes, server-side parsing for bundle control, clean build, and DevSov validation on `localhost:3002`.
2026-04-13 16:47 | UI parity fix: added dedicated brand-detail experience, locale-safe homepage parser for `en/hy/ru`, restored source services/localized headings, softened hover system, strengthened homepage scene-scroll, added moving light-gradient, clean build, fresh DevSov on `localhost:3003`.
2026-04-13 18:35 | Lag/scroll fix: reduced Lenis/ScrollTrigger cost, limited parallax to key zones, made homepage light-layer visible, compacted `Our Partners`, rebuilt partner detail media composition, fixed broken legacy media src for dev runtime, validated DevSov on `localhost:3006`; `npm run build` still hangs before first compile step.
2026-04-13 19:37 | Cleanup/local-run: proved legacy parity (`129/129` routes), restored runtime coverage for missing home/vacancy/sladonezh media, archived `modern-app`/`test-results`/`optimization-*`, deleted legacy route HTML, added portable `runLocal` + safe cleanup scripts, rewired `devSov` to Next.js wrapper, validated `lint`/`build`/`runLocal`/`devSov`.
2026-04-13 19:45 | Commands: replaced PowerShell `devSov`/`pushSov` with `devNew`/`pushNew`, kept `depSov` unchanged, pointed git push helper to `https://github.com/shaburyaan/SovWeb-2.0`, updated quick-start/docs.
2026-04-13 19:58 | Deploy fix: switched Cloudflare deploy flow from uploading repo root to building static Next.js export (`out/`), added build/deploy Pages script, prerendered locale/slug routes via `generateStaticParams`, preserved `_headers`/`_redirects`, validated `npm run build:pages`.
2026-04-13 20:07 | Deploy gaps fix: enabled export-safe images, copied referenced runtime assets into `out/`, normalized transition pathname compare for trailing-slash export, replaced homepage media/testimonial lorem with localized fallback content, validated `build:pages`, asset HTTP `200`s, and locale/brand static routes.
2026-04-13 23:43 | Roboto/media pass: switched UI typography to `Roboto` with `Noto Sans Armenian` fallback, replaced header wordmark with image-logo, tightened cursor follow, generated manifest-backed `optimized-media` webp assets for homepage/partners/detail/distribution surfaces, enlarged media slots with local `contain` fixes, and revalidated `build:pages`.
2026-04-14 11:25 | Nav/partners fix: switched header logo to cropped optimized asset, rebuilt header grid so nav/locales no longer collapse around the logo, normalized supplemental partner cards through `getOptimizedAssetSrc()`, added missing `2022/09` manifest mappings for Mamont/White Birch detail media, and revalidated `npm run build:pages`.
2026-04-16 10:18 | Media/gradient/overlay fix: normalized homepage/generic/detail/vacancy hero-media through `getOptimizedAssetSrc()`, extended optimized-media coverage for remaining raw locale and legacy assets, upgraded `prepare-optimized-media.py` with manifest-merge + internal white-island cleanup and regenerated targeted/logo assets, added a visible global light layer plus stronger homepage glow treatment, hardened page-transition teardown with fail-safe reset, and revalidated `npm run build:pages` with no raw image preloads left in `out/**/*.html`.
2026-04-16 12:31 | Visual hotfix pass: rebuilt root light-layer stacking and darkened false-white surfaces, routed homepage partners and all parallax media through manifest-backed optimized assets, split locale routing into explicit `hy/ru` pages plus stable english `[slug]`, added targeted `2022/11` Russian Standard media cleanup/mappings, and live-validated `http://localhost:3000`, `/our-partners`, `/garofalo`, `/virgilio` with route `200`s and no visible white card/background blocks in the checked views.
2026-04-16 13:36 | Partners marquee pass: replaced `Our Partners` photo-hero with CSS-only 3-row real-logo marquee, resolved partner cards to logo-priority assets, removed dense card text-splitting on partners grid, limited heavy foreground light to locale home routes only, validated `npm run build`, warm local timings, fresh dev `3002` for `/` + `/our-partners`, and static export `3004` for `/our-partners`, `/hy/our-partners`, `/ru/our-partners`, `/garofalo`; stale Windows `.next` cache still needs clean restart if dev shows transient `500`.
2026-04-16 14:12 | Perf cleanup pass: throttled Lenis->ScrollTrigger sync, stopped idle cursor RAF churn, reduced global blur/backdrop/light-layer cost, trimmed magnetic usage on nav/partners tabs, weakened scrub/parallax intensity and non-hero char splitting, added ignores for local build/test/screenshot artifacts, validated `lint`/`build`/`build:pages`, and checked static `3004` routes for `/`, `/our-partners`, mobile `our-partners`, `/garofalo`.
2026-04-16 15:14 | Deploy stability fix: made `deploy-pages.mjs` wipe stale `.next`/`out` before build, aligned normal `next build` with the stable debug-prerender path via `serverMinification: false`, `prerenderEarlyExit: false`, and server sourcemaps, then revalidated `npm run build:pages` twice on Windows with successful static export.
2026-04-16 17:25 | Homepage fullpage pass: cut duplicated home glow/backdrop costs, disabled Lenis on locale home routes, replaced scrub-heavy homepage flow with a custom section-by-section controller (wheel/key/touch + anchors + bullets) instead of `react-fullpage` due Next/license risk, fixed Windows `build:pages` nested-build instability by invoking Next CLI directly from `deploy-pages.mjs`, and revalidated `lint` plus static export/screens for `/`, `/#partners`, mobile home, and `/garofalo`.
2026-04-17 08:56 | Homepage rollback pass: removed the active homepage fullpage controller wrapper and returned locale home routes to the plain scene flow, cut home backdrop light rendering plus home hero zoom animation, removed home locale `accent` hero overrides so only the seated-people background remains, and revalidated `lint`/`build`/`build:pages` with fresh static `200` checks for `/`, `/our-partners`, and `/garofalo` plus rollback screenshots.
2026-04-17 10:07 | Cleanup: deleted confirmed local junk only (`artifacts/`, `page-bg/`, root visual/perf screenshots, root duplicate `Sovrano Main Logo 1.png`, archived `_archive/` snapshot), kept runtime/source/media pipeline files intact, and expanded `.gitignore` to stop these artifacts from reappearing.
